#include <bits/stdc++.h>
#define EPS 1e-9
#define PI 3.14159265359

typedef long long ll;

using namespace std;

int main()
{
    int n;
    while(1){
        scanf("%d", &n);
        if(!n) return 0;
        if(n > 100) printf("f91(%d) = %d\n", n, n - 10);
        else printf("f91(%d) = 91\n", n);
    }
}

